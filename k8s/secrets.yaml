apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: knowledge-graph
type: Opaque
stringData:
  # Fuseki Credentials
  FUSEKI_USERNAME: "admin"
  FUSEKI_PASSWORD: "CHANGE_IN_PRODUCTION_123"
  
  # PostgreSQL Credentials
  POSTGRES_USER: "ontology_user"
  POSTGRES_PASSWORD: "CHANGE_IN_PRODUCTION_456"
  POSTGRES_DB: "ontology_db"
  
  # Redis Password (optional)
  REDIS_PASSWORD: ""
  
  # API Secret Key
  API_SECRET_KEY: "CHANGE_THIS_TO_RANDOM_SECRET_KEY"
  
  # JWT Secret
  JWT_SECRET_KEY: "CHANGE_THIS_TO_RANDOM_JWT_SECRET"
